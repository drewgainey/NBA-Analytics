CREATE TABLE nba_rankings (
    team_id VARCHAR(255),
    name VARCHAR(255),
    market VARCHAR(255),
    sr_id VARCHAR(255),
    reference VARCHAR(255),
    conference_rank VARCHAR(255),
    division_rank VARCHAR(255)
);